version: '3.8'

services:
  n8n:
    image: n8nio/n8n
    container_name: n8n
    restart: unless-stopped
    ports:
      - "5678:5678"
    environment:
      - N8N_LOG_LEVEL=info
    volumes:
      - n8n-data:/home/node/.n8n

volumes:
  n8n-data:
    driver: local

